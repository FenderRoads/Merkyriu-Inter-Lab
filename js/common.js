$(function() {
  // Slider

  $(".carousel1").owlCarousel({
    loop: true,
    dots: true,
    autoplay: true,
    autoplay: false,
    autoplayTimeout: 5000,
    autoplayHoverPause: false,
    smartSpeed: 500,
    responsive: {
      0: {
        items: 1
      }
    }
  });

  // Slider_
});
